<UserControl x:Class="DrawingManagerApp.Views.FileFoldersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DrawingManagerApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="517">

    <Grid Background="white">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button Grid.Row="0" Grid.Column="1" Content="Save Changes" Command="{Binding SaveFolderPathCommand}"/>
        <DataGrid Grid.Row="1" ItemsSource="{Binding FolderPathList}" SelectedItem="{Binding SelectedFolderPath}"/>        
    </Grid>
</UserControl>
